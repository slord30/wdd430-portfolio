import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // Place allowedDevOrigins directly at the root level:
  allowedDevOrigins: ['192.168.5.204'],
};

export default nextConfig;
